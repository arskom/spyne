language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  # - "3.5"
  - "pypy"
before_install:
  - sudo apt-get install -qq libzmq3-dev
install:
  - pip install tox
script:
  - python$TRAVIS_PYTHON_VERSION setup.py test

  # disable pypy -- travis' pypy deployment seems broken
  # - if [[ $TRAVIS_PYTHON_VERSION == 'pypy' ]]; then tox -e pypy; fi
